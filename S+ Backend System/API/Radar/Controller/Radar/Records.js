const {
  listDistance,
  createDistance,
  computeDistance,
} = require("../Calculator/Distance");
const { listSpeed, createSpeed, computeSpeed } = require("../Calculator/Speed");
const {
  listIdentification,
  createIdentification,
  computeIdentification,
} = require("../Identification/Identification");
const { listRadar, createRadar } = require("./Radar");

module.exports = {
  listDistance,
  createDistance,
  computeDistance,
  listSpeed,
  createSpeed,
  computeSpeed,
  listIdentification,
  createIdentification,
  computeIdentification,
  listRadar,
  createRadar,
};
